name: Deploy to GitHub Pages

<<<<<<< HEAD
permissions:
  contents: read
  pages: write
  id-token: write # IDトークンを書き込むための権限

=======
>>>>>>> 43cc07438c00dad52046dc8ec79af7cdf571b356
on:
  # mainブランチにプッシュがあった時にこのワークフローを実行する
  push:
    branches:
      - main
  # GitHub上で手動実行できるようにする
  workflow_dispatch:
<<<<<<< HEAD
    
=======

# 実行中のワークフローが重複しないようにする設定
concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow }}-${{ github.ref }}

>>>>>>> 43cc07438c00dad52046dc8ec79af7cdf571b356
jobs:
  # 1. ビルドジョブ
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20 # お使いのNode.jsバージョンに合わせてください
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build Project (Vite)
        # Viteのビルドコマンドを実行
        run: npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # ビルドされた静的ファイルが入っているフォルダを指定
          path: './dist'
  
  # 2. デプロイジョブ (GitHub Pages専用)
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build # buildジョブが完了してから実行
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
<<<<<<< HEAD
        uses: actions/deploy-pages@v4
=======
        uses: actions/deploy-pages@v4
>>>>>>> 43cc07438c00dad52046dc8ec79af7cdf571b356
